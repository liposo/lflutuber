name: iflutuber

description: A Flutter application for searching Youtube videos.

publish_to: none  # This app is not intended for publishing

version: 1.0.0

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.5
  video_player: ^2.8.3
  flutter_bloc: ^8.1.5
  bloc: ^8.1.4
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
# Optional: Add icons for search and video play functionality (replace with desired packages)
# -cupertino_icons: ^1.1.3  # For iOS-style icons

# ... (other sections if needed)
